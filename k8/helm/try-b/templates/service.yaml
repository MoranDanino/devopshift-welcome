apiVersion: v1
kind: Service
metadata:
  name: {{ include "try-b.fullname" . }}
  labels:
    {{- include "try-b.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "try-b.selectorLabels" . | nindent 4 }}
