apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "try-b.fullname" . }}-test-connection"
  labels:
    {{- include "try-b.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "try-b.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
