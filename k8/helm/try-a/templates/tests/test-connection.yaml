apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "try-a.fullname" . }}-test-connection"
  labels:
    {{- include "try-a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "try-a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
