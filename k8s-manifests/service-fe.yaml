apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort # No need to use LB OR BROWSE THE APPLICATION 
  ports:
    - port: 5011
      targetPort: 5011
  selector:
    app: fe